---
description: Reviews dormant leads and previous clients, schedules context-based follow-up sequences and writes today's drafts. Sends nothing.
---

# /follow-up

Use the project-local `upwork-follow-up` skill and complete its full workflow.
That workflow uses the project-local `upwork-copy` skill for every draft.

Open with the ROADMAP required by `CLAUDE.md`. The member does not need to make a
decision during the run. End with the required completion report and exact
Upwork call count. Nothing is sent by this command.
